name: CD

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Log in to Docker Hub
      uses: docker/login-action@v1
      with:
        username: ${{ secrets.vijayvithal410039 }}
        password: ${{ secrets.Vijay@vithal2019 }}

    - name: Build and push Docker image
      run: |
        docker build -t vijayvithal410039/my_flask_app:latest .
        docker push vijayvithal410039/my_flask_app:latest
